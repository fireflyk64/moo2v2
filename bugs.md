- [x] The Galaxy Waits on you screen shouldn't be present in a bot game: It also shouldn't take up a new line. Can it be red covering the chat screen instead? That way it doesn't shift the display all around whenever it appears and people shouldn't be chatting if they are the last. Though ideally you could still click through it semi-transluencttly to chat — FIXED: the banner is gone; a translucent red wash (`pointer-events: none`, so clicks reach the chat underneath) now overlays the chat footer instead — zero layout shift. It never renders in solo games (bots commit instantly, so it was true every turn = pure noise). GameShell.svelte `data-testid="all-waiting"`.
- [x] There should be a hotkey to select all ships when you order ships around. Right now I click all ships then have to slowly select over to  move one. Maybe by default the "all" checkbox coulud and users can toggle individual ships and cycle through with a hotkey like backspace to smooth out the fleet movement and make it more fun and less like a checkmark game. — FIXED: on the map, **A** selects all/none of your ships at the selected star and **Backspace** cycles ship 1 → ship 2 → … → all (hint chip in the "Your ships here" header). Default-select-all was deliberately skipped: with ships selected, clicking any star issues a move, so it would turn map browsing into accidental fleet orders — A gives one-keystroke select-all instead.
- [x] Autopilot should only be active when select all is checekd to avoid cluttering screen space — FIXED: the 🎚 autopilot bar only appears when every colony is selected (select all) — or while autopilot is already ON, so a running governor can always be seen and switched off.
- [x] I don't see that I can buy soil enrichment in my terran or arid colonies. That would be huge — FIXED: the engine was right (terran/arid always allowed it once Advanced Ecology is researched) — the bulk "set build for all…" menus showed only the INTERSECTION of the selection's buildables, so one barren or already-enriched colony hid soil enrichment for every world that wanted it. The menus now offer the union, labeled "Soil Enrichment (7 of 12)" when partial; colonies that can't take an item are skipped as before.
- [x] Also the research percentage isn't the percentage of actually getting the research this turn. I think it's only percent of the minimum bar — FIXED: readouts now say "N% of base" while banked RP is under the listed cost, then switch to the true "N% chance" of discovering by next turn (the discovery line hides between 1× and 2× the listed cost; the Research screen already computed these odds — the toolbar and colony readouts now use them too).
- [x] When there is no defender, defender should not have to pick from the screen. The battle replay showing noonne isn't ideal. Maybe it can show bombardment if the aggressor selected to do so. — FIXED (ENGINE 0.16.0): a defender with no warships and no fighting base/batteries gets passive orders pre-filled (like NPC sides) and is never prompted; walkovers emit no replay at all — the battle report still lands with the bombardment results (pop killed, buildings destroyed). Bonus: fixed a detection bug where a lower-id fleet catching a naked convoy at a neutral star was labeled the "defender" and the battle silently never happened.
- [x] System administrators should affect the whole starsystem not just one colony — FIXED (ENGINE 0.16.0): colony leaders are system administrators now — their colony-scope bonuses (farming/labor/science/financial/spiritual/medicine…) reach every colony you own in the assigned star system; leaders in one system stack; a captured seat stops administering. Tests: tests/unit/leaders.test.ts.
- [x] if a battle is lost with combatant ships and non-combatant ships, the noncombatant ships should be allowed to flee to the nearest colony (that wasn't the one being attacked). It can be in the same system. If there were combatant ships in a battle no non-com ships should be lost in a space fight — FIXED (ENGINE 0.16.0): if the losing side fielded ANY battle line (ships or its defense base, even if wiped out), its non-combatants flee to the nearest own colony other than the contested one (staying only if no other haven exists). Only non-coms caught with no battle line at all are run down. Tests: tests/unit/battle-noncoms.test.ts.
