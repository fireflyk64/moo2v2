1) when the popup "the galaxy awaits you" show up, you can't scroll sideways.
2) against hte computer that galaxy awaits you banner is always up despite it being meanningless against the computer--it destroys the theme
3) it'd be great if when you're in the main map, and you click on a star system, then you can click on the planet to open up the planet screen and you can manage some production there. If it's too hard to have a popup colony screen and absolutely necessary necesasry go to the colony screen and sort it to the top. but it does break the flow--instead a popup table for one world would be ideal. Without this fix, right now, you'd have to look up the name, then go to the colonies screen. 
4) should the queue just within the building  dropdown? when you click on the dropdown, you can see the current queue and add or move things around there?
5) theres a bug where if you build something, and don't have something else queue, all your production that overflows / saved will be lost. it shoudl be ready to be reallocated for the next turn on whatever you have queued
6) Please try the async variant in a large galaxy and have different bots play several different species and generate 5 savegames. Then try a few reconciliation runs of 500 turns each. Put the savegames into an example folder in ~/examplesaves. For the automatic reconiliation, Give each different AI parameters and try to tune each one so it beats the last, making the AI better each time by tuning parameters and finding new strategies.
7) test those 5 games in an interactive reconcilation round and ensure the game is playable
8) (playtest, Hai Nguyen) it'd be useful to know what the leader actually does — leader cards showed bare skill names with no explanation, and Director Androgena/Brainac had a blank epithet line ("she has no attribute lol"). FIXED: every skill chip now has a hover description with the real magnitude at the leader's level (ship-only combat skills are flagged as decorative on colony leaders), both Directors got epithets, unassigned colony leaders warn that their bonuses are idle, and the ? help explains leader scope + salaries.
9) (playtest, Hai Nguyen) research queue bug: clicking a further-down tech looks like it queues the path, but it never selects the research. FIXED: queueing a deep field now queues its whole unresearched prerequisite ladder ahead of it (tree node names are also clickable now); the drain self-heals queues saved before the fix.
10) (playtest, Hai Nguyen) "sometimes my leaders just vanish… we should have a warning". Leaders quit silently the moment the treasury goes negative (most expensive first). FIXED: a warning banner appears the turn BEFORE projected insolvency, auto-play stops both before and after a quit, and leader_quit now shows under the economy/danger report filters in red.
11) (playtest, save 0a3b3533 turn 92) "Has something broke with battles? I haven't been able to watch them (vs monsters)". DIAGNOSIS: two things stacked. In the attached save every monster encounter was a lone scout the player ordered to evade (replay exists but is ~8 ticks of a scout fleeing). Systemically, since 0.22.0 any fleet massing 12+ hull-weight vs an ordinary lair hits the deterministic instant clear, which returned BEFORE the battle_replay event was pushed — the orders dialog asked for a stance and then no battle ever appeared, so a strong fleet could NEVER watch a monster fight. FIXED (0.34.0): attacker battle orders may carry fightOut: true — the real sim runs and emits the replay. The orders dialog ALWAYS sends it when the clear would trigger (no checkbox: a guaranteed-lossless option would out-incentivize ever fighting — the instant clear stays bot/timeout-only); absent = the lossless instant clear, byte-exact for old logs/bots/timeouts. Reports now label auto-cleared battles. Regression: tests/unit/monsterwatch.test.ts.
12) (playtest) freighter chip read "10/10" (free/total) while 6 hauled food — actual usage was only in the turn report. FIXED: the top bar now shows freightersInUse/total (engine EmpireSummary.freightersInUse, the same number freighter upkeep charges for), tooltip rewritten.
13) (playtest) "set build for all" on a multi-selection deleted the existing queues (new tech lands → set it everywhere → everything queued is gone). FIXED: renamed "build now for all…" — the pick goes to the head and the ENTIRE existing queue stays behind it (capped at 12); invested production already carried over via the engine.
14) (playtest) enabling auto management redid all the food jobs, and disabling didn't restore them. FIXED: while no colony actually lacks food, the governor now preserves every colony's farmer assignment exactly (presetJobs keepFarmers mode) and only rebalances workers/scientists; farmers are re-planned only when someone is genuinely short. It also stops submitting identical set_jobs every pass. Regression: tests/unit/foodjobs.test.ts (incremental presets).
15) (playtest) hypothesis: pop at 1.95 grows past 2.0, a food shortage subtracts, and a FULL unit is wrongly lost. VERIFIED NOT A BUG: starvation removes 50 popK (0.05 units) per food unit short (economy.ts groupGrowthK); the "2 → 1" was integer flooring in the display (2.05 shows "2", 1.95 shows "1"). The colonies table now shows one decimal (6.4/12) so fractional dips are visible; pinned by tests/unit/popfraction.test.ts.
16) (playtest) option to play without leaders. ADDED: modes.noLeaders game-setup checkbox (lobby + day-one form) — leadersUpkeep never rolls an offer, the Guardian bounty skips Loknar (death ray stays), the Leaders section hides. Regression: tests/unit/leaderoffers.test.ts.
17) (playtest) "map initial starting conditions might not be super fair — in some maps you could be so far away from other planets". MEASURED (30 medium 2-player maps): nearest colonizable star per home ranges 152-530cp; within one map the two players' gaps differ by up to 310cp (p90 241cp), and good-climate stars within 500cp can split 0 vs 2 or 4 vs 1. Home systems themselves are identical and homes are 900cp apart, but nothing constrains the NEIGHBORHOOD. FIXED (0.34.0): home selection now draws up to 12 valid well-separated sets and keeps the one with the smallest between-home spread of nearest-real-planet distance (ties: smallest worst case). Re-measured on the same 30 maps: same-map spread fell p50 79→7cp, p90 241→22cp, max 310→132cp (4p large mean 133→105cp — monsters land after selection, so residual noise remains). The 900cp pairwise home separation is untouched; mirror mode still gives perfect symmetry. Regression: tests/unit/galaxygen.test.ts.
